{
  "name": "cloudflare-jwt-verifier",
  "version": "1.0.0",
  "description": "Cloudflare Worker JWT Verifier",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cloudflare",
  "license": "MIT",
  "devDependencies": {
    "serverless-cloudflare-workers": "1.2.0",
    "serverless-scriptable-plugin": "^1.0.0",
    "webpack-cli": "^3.3.0"
  },
  "dependencies": {
    "jsonwebtoken": "^8.5.1"
  }
}
